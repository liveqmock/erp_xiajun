<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangqin.mapper.ChannelMapper">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id, gmt_create AS gmtCreate, gmt_modified AS gmtModified, `name`, `type`, discount, contact_mobile AS
        contactMobile, contact_name AS contactName, contact_email AS contactEmail, contact_url AS contactUrl, remark,
        sale_level AS saleLevel,discount1,discount2,discount3
    </sql>

    <select id="queryChannelList" resultType="com.wangqin.model.Channel">
        SELECT
        <include refid="Base_Column_List"/>
        FROM channel T1
        ORDER BY T1.id desc
    </select>
</mapper>