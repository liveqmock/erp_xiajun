<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangqin.globalshop.item.dal.mapperExt.ItemPackagePatternMapperExt">
	
	<sql id="Base_Column_List">
		id,pattern_name AS name
	</sql>
	
	<!--  
	<select id="queryPackageLevelsCount" resultType="java.lang.Integer"  parameterType="com.wangqin.globalshop.biz1.app.vo.ShippingPackingPatternQueryVO">
        SELECT
        	count(1)
        FROM
            `package_level` T1
        where
     		   1=1
            <if test=" name != null and name != '' ">
                and T1.name = #{name}
            </if>
            <if test=" packageEn != null and packageEn != '' ">
            	and T1.package_en = #{packageEn}
            </if>
            <if test=" packageLevel != null ">
            	and T1.package_level = #{packageLevel}
            </if>
            <if test=" packageId != null ">
            	and T1.package_id = #{packageId}
            </if>
            
    </select>
    
    <select id="queryPackageLevels" resultType="com.wangqin.globalshop.biz1.app.dal.dataObject.ShippingPackingPatternDO" parameterType="com.wangqin.globalshop.biz1.app.vo.ShippingPackingPatternQueryVO">
        SELECT
        	 <include refid="Base_Column_List" />
        FROM
            `shipping_packing_pattern` T1
        where
             T1.is_del=0
            <if test=" name != null and name != '' ">
                and T1.name = #{name}
            </if>
            <if test=" packageEn != null and packageEn != '' ">
            	and T1.name_en = #{packageEn}
            </if>  
            <if test=" packageLevel != null ">
            	and T1.package_level = #{packageLevel}
            </if>
           
            <if test=" packageId != null ">
            	and T1.package_id = #{packageId}
            </if>
          order by T1.id 
          	<![CDATA[ LIMIT #{firstStart},#{pageSize} ]]>
    </select>
    -->
    
    <select id="queryPatternsByScaleNo" resultType="com.wangqin.globalshop.item.app.dal.dto.ItemPackagePatternDTO"  >
        SELECT
        	 <include refid="Base_Column_List" />
        FROM
            `shipping_packing_pattern` T1
        where
     		  
            T1.packaging_scale_no=#{no}
            
    </select>
    
</mapper>