<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangqin.mapper.SiteMsgMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		T1.id, T1.gmt_create AS gmtCreate, T1.gmt_modified AS gmtModified, T1.title, T1.content, T1.`status`, T1.send_time AS sendTime, T1.send_type AS sendType
	</sql>


	
	<!-- 根据status和send_type读取站内信,XiaJun -->
	<select id="queryMsgByStatusSendType" resultType="com.wangqin.model.SiteMsg" parameterType="com.wangqin.vo.SiteMsgQueryVO">
		SELECT 
		<include refid="Base_Column_List" />
		FROM site_msg T1 
		WHERE 
		1=1
		<if test = "status != -1">
			AND T1.status = #{status}
		</if>
		<if test = "sendType != -1">
			AND T1.send_type = #{sendType}
		</if>
		 
     	ORDER BY T1.id desc
     	<![CDATA[ LIMIT #{firstStart},#{pageSize} ]]>
	</select>
	
	<!-- 获取最大主键,XiaJun -->
	<select id="getMaxId" resultType="java.lang.Long" >
		SELECT 
		IFNULL(max(T1.id),0)
		FROM site_msg T1 
		
	</select>
</mapper>